
import { createTheme }  from '@mui/material/styles'
import '../style.css'
export const theme = createTheme({
  palette: {
    primary: {
        main: "#7286D3"
    },
    secondary: {
        main: "#E5E0FF"
      },     
    typography:{
      allVariants:{
      fontFamily:'Mynerve,cursive'
}    },
  }
});